**CRTR Marquee** — scrolling capability strip (ink band, paper text, red star separators); pauses on hover and respects reduced-motion.

```jsx
<Marquee items={['Video editing','Motion design','Web development','Brand systems']} />
```

`speed` is seconds per loop (lower = faster). Swap the separator with `star`.
