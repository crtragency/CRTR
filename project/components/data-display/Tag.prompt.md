**CRTR Tag** — outlined mono chip for listing capabilities; borders use currentColor so it inverts cleanly on dark surfaces.

```jsx
<Tag>After Effects</Tag> <Tag>Lottie</Tag>
```

No variants — set color via the parent's `color`.
