**CRTR Input** — structural text field with a mono label and red focus ring.

```jsx
<Input label="Email" type="email" placeholder="you@studio.com" />
<Input label="Budget" error="Required" />
```

Pass `label`, `hint`, or `error` (turns border/ring red). Forwards all native input props.
