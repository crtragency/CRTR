**CRTR Textarea** — multi-line companion to Input with identical structural styling.

```jsx
<Textarea label="Tell us about the project" rows={5} />
```

Same `label` / `hint` / `error` props as Input.
